const fs = require('fs'); let code = fs.readFileSync('backend/src/scripts/test-prn.ts', 'utf8'); code = code.replace(/\/\/ Find the first radio[\s\S]+?if \(radio\) radio\.click\(\);/, 'const radio = document.querySelector(\'input[name="optradio"], input[name="rdoGrp"], input#optradio, input[type="radio"][value="0"]\') as HTMLElement; if (radio) radio.click();'); fs.writeFileSync('backend/src/scripts/test-prn.ts', code);
