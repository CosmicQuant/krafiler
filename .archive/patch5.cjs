const fs = require('fs'); let code = fs.readFileSync('backend/src/scripts/test-prn.ts', 'utf8'); code = code.replace('const addBtn = document.querySelector(\'input[value="Add"], input[name="btnAdd"], button#addBtn, input.addButton\') as HTMLElement;', 'const addBtn = Array.from(document.querySelectorAll('a, button, input')).find(el => el.textContent?.trim() === "Add" || el.value === "Add" || el.innerHTML?.includes("Add")) as HTMLElement;'); fs.writeFileSync('backend/src/scripts/test-prn.ts', code);
