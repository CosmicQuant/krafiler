const fs = require('fs'); let code = fs.readFileSync('src/scripts/test-prn.ts', 'utf8'); code = code.replace(/select\[name="taxHeadId"\], select#taxHead, select\[name="taxHead"\]/g, 'select#cmbTaxHead'); code = code.replace(/select\[name="taxSubHeadId"\], select\[name="taxSubHead"\]/g, 'select#cmbTaxSubHead'); code = code.replace(/select\[name="paymentTypeId"\], select\[name="paymentType"\]/g, 'select#cmbPaymentType'); code = code.replace(/select\[name="taxPeriodYear"\]/g, 'select#cmbTaxPeriodYear'); code = code.replace(/select\[name="taxPeriodMonth"\]/g, 'select#cmbTaxPeriodMonth'); code = code.replace(/input\[value="Add"\], button:has-text\("Add"\)/g, 'input[value="Add"], input[name="btnAdd"], button#addBtn, input.addButton, button:has-text("Add")'); fs.writeFileSync('src/scripts/test-prn.ts', code);
